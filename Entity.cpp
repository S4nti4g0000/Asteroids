#include "EntityFr.hpp"

/*EntityFr::EntityFr(TransformComponent* transform, SDL_Texture* texture, Manager* manager, float velocity)
	:tCompo_(transform), entTexture(texture), mngr_(manager), velocity_(velocity), aliveBoi(true)
{
	pos = &(tCompo_->getPosition());
	//cFrame = &(tCompo_->Render());
}
*/